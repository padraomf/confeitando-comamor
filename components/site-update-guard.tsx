// Runs before hydration, including when an old page references a removed bundle.
const guard = `(()=>{let shown=false;function show(){if(shown)return;shown=true;function mount(){const box=document.createElement('div');box.setAttribute('role','alert');box.style.cssText='position:fixed;top:12px;left:12px;right:12px;z-index:2147483647;padding:16px;background:#fff6f0;color:#694736;border:1px solid #ab3858;border-radius:10px;font:14px/1.5 sans-serif;box-shadow:0 4px 18px #0002';const message=document.createElement('p');message.textContent='O site recebeu uma atualização ou a conexão foi interrompida. Atualize a página para continuar.';const button=document.createElement('button');button.textContent='Atualizar página';button.style.cssText='margin-top:8px;padding:10px 16px;background:#ab3858;color:white;border:0;border-radius:6px;cursor:pointer';button.onclick=()=>location.reload();box.append(message,button);document.body.append(box)}if(document.body)mount();else document.addEventListener('DOMContentLoaded',mount,{once:true})}addEventListener('error',event=>{const el=event.target;if((el instanceof HTMLScriptElement||el instanceof HTMLLinkElement)&&String(el.src||el.href||'').includes('/_next/static/'))show()},true);addEventListener('unhandledrejection',event=>{if(/Failed to fetch dynamically imported module|Importing a module script failed|Loading chunk .* failed|ChunkLoadError/.test(String(event.reason&&event.reason.message||event.reason||'')))show()})})();`;
export function SiteUpdateGuard() {return <script dangerouslySetInnerHTML={{__html: guard}}/>;}
