services:
  whatsapp:
    build: .
    restart: unless-stopped
    init: true
    shm_size: 512mb
    volumes:
      - whatsapp-data:/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
volumes:
  whatsapp-data:
